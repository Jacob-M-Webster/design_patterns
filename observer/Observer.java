package observer;
/**
 * Observer interface | provides the abstract classes required by all observers
 * @author Jacob Webster
 */
public interface Observer {
    public void update(boolean warning, int note);
}
